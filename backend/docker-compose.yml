services:
  postgres:
    image: postgres:16
    container_name: ticketing-db
    env_file:
      - .env
    ports:
      - "5432:5432"
    volumes:
      - ticketing-data:/var/lib/postgresql/data

volumes:
  ticketing-data:
